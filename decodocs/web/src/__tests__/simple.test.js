import { describe, test, expect } from 'vitest';

// Simplified test without routing dependencies
describe('Simple Test', () => {
  test('should render without crashing', () => {
    // Just a simple test to check if the testing setup works
    expect(1).toBe(1);
  });
});